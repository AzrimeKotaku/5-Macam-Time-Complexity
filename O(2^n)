#include<stdio.h>

int fib(int n);

main(){
	int a;
	printf("");
	scanf("%d", &a);
	fib(a);
 }
  
int fib(int n) {
	if(n == 0)
		return 0;
	else if(n == 1)
		return 0;
	else
	return fib(n - 1) + fib(n - 2);
}
