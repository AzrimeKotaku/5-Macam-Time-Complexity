#include<stdio.h>

int A(int n);

main(){
	int n;
	printf("");
	scanf("%d", &n);
	A(a);
 }
  
int A(int n) {
	if(n == 0)
		return 0;
	else if(n == 1)
		return 0;
	else
	return fib(n - 1) + fib(n - 2);
}
